apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-dns
resources:
  - ../../../applications/external-dns-gateway-api

patches:
- patch: |
    - op: add
      path: /spec/template/spec/containers/0/args/0
      value: --txt-owner-id=argocd.rspot.rye.ninja
  target:
    group: apps
    version: v1
    kind: Deployment
    name: external-dns
