apiVersion: kustomize-krm-envsubst/v1alpha
kind: Envsubst
metadata:
  name: envsubst
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: /usr/local/bin/kustomize-krm-envsubst
includedVariableNames:
  - K_ZONE_NAME
  - K_INFRA_NAME
values:
  K_ZONE_NAME: rye.ninja
  K_INFRA_NAME: rspot