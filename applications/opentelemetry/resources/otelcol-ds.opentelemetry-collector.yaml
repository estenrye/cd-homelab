apiVersion: opentelemetry.io/v1beta1
kind: OpenTelemetryCollector
metadata:
  name: otelcol-ds
spec:
  mode: daemonset
  terminationGracePeriodSeconds: 600
  hostNetwork: true
  image: otel/opentelemetry-collector-k8s:0.139.0
  serviceAccount: opentelemetry-collector
  env:
    - name: K8S_NODE_NAME
      valueFrom:
        fieldRef:
          fieldPath: spec.nodeName
  ports:  
    - name: metrics
      port: 8888
      hostPort: 8888
      protocol: TCP
      targetPort: 8888
    - name: otlp-grpc
      port: 4317
      hostPort: 4317
      protocol: TCP
      targetPort: 4317
    - name: otlp-http
      port: 4318
      hostPort: 4318
      protocol: TCP
      targetPort: 4318
    - name: envoyals
      port: 4319
      hostPort: 4319
      protocol: TCP
      targetPort: 4319
  volumeMounts:
    - name: hostfs
      mountPath: /hostfs
      readOnly: true
      mountPropagation: HostToContainer
  volumes:
    - name: hostfs
      hostPath:
        path: /
  config:
    receivers:
      # Receive metrics from the Envoy Access Log Service
      # https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/receiver/envoyalsreceiver
      # https://gateway.envoyproxy.io/v1.5/tasks/observability/proxy-accesslog/#ggrpc-access-log-serviceals-sink
      # Needs custom otelcollector image with the envoyalsreceiver included
      # https://github.com/open-telemetry/opentelemetry-collector/tree/main/cmd/builder
      # envoyals:
      #   endpoint: 0.0.0.0:4319
      hostmetrics:
        root_path: /hostfs
        scrapers:
          cpu:
            metrics:
              system.cpu.time:
                enabled: true
              system.cpu.frequency:
                enabled: false
              system.cpu.logical.count:
                enabled: true
              system.cpu.physical.count:
                enabled: false
              system.cpu.utilization:
                enabled: true
          disk:
            metrics:
              system.disk.io:
                enabled: false
              system.disk.io_time:
                enabled: false
              system.disk.merged:
                enabled: false
              system.disk.operation_time:
                enabled: true
              system.disk.operations:
                enabled: false
              system.disk.pending_operations:
                enabled: false
              system.disk.weighted_io_time:
                enabled: false
          # filesystem:
          load:
            metrics:
              system.cpu.load.average_1m:
                enabled: true
              system.cpu.load.average_5m:
                enabled: true
              system.cpu.load.average_15m:
                enabled: true
          # memory:
          # network:
          # process:
          # processes:
          # paging:
      kubeletstats:
        collection_interval: 10s
        auth_type: 'serviceAccount'
        endpoint: '${env:K8S_NODE_NAME}:10250'
        insecure_skip_verify: true
        metric_groups:
          - node
          - pod
          - container
      otlp:
          protocols:
            grpc:
              endpoint: 0.0.0.0:4317
            http:
              endpoint: 0.0.0.0:4318
    processors:
      resource:
        attributes: []
      metricstransform/rename:
        transforms: []
      filter/ottl:
        error_mode: "ignore"
        traces:
          span: []
          spanevent: []
        metrics:
          metric: []
          datapoint: []
        logs: 
          log_record: []
        profiles:
          profile: []
      transform/metrics:
        error_mode: "ignore"
        metric_statements: []
      memory_limiter:
        check_interval: 1s
        limit_percentage: 75
        spike_limit_percentage: 15
      resourcedetection/system:
        detectors: [k8snode, docker, system]
        system:
          hostname_sources: ["os"]
          resource_attributes:
            host.arch:
              enabled: true
            host.cpu.cache.l2.size:
              enabled: false
            host.cpu.family:
              enabled: false
            host.cpu.model.id:
              enabled: false
            host.cpu.model.name:
              enabled: true
            host.cpu.stepping:
              enabled: false
            host.cpu.vendor.id:
              enabled: false
            host.id:
              enabled: false
            host.interface:
              enabled: false
            host.ip:
              enabled: false
            host.mac:
              enabled: false
            host.name:
              enabled: false
            os.build.id:
              enabled: true
            os.description:
              enabled: false
            os.name:
              enabled: false
            os.version:
              enabled: false
            os.type:
              enabled: true
        k8snode:
          resource_attributes:
            k8s.node.name:
              enabled: true
            k8s.node.uid:
              enabled: true
      k8sattributes:
        auth_type: 'serviceAccount'
        extract:
          metadata: # extracted from the pod
            - k8s.namespace.name
            - k8s.pod.name
            - k8s.pod.start_time
            - k8s.pod.uid
            - k8s.deployment.name
            - k8s.node.name
        pod_association: # How to associate the data to a pod (order matters)
          - sources: # First try to use the value of the resource attribute k8s.pod.ip
              - from: resource_attribute
                name: k8s.pod.ip
          - sources: # Then try to use the value of the resource attribute k8s.pod.uid
              - from: resource_attribute
                name: k8s.pod.uid
          - sources: # If neither of those work, use the request's connection to get the pod IP.
              - from: connection


    exporters:
      debug:
        verbosity: detailed

    service:
      pipelines:
        metrics:
          receivers: [otlp, hostmetrics]
          # receivers: [otlp, hostmetrics, kubeletstats]
          # Need custom otelcollector image with the envoyalsreceiver included to enable this
          # receivers: [kubeletstats, otlp, hostmetrics,  envoyals]
          processors: [resource, resourcedetection/system, k8sattributes, memory_limiter, metricstransform/rename, transform/metrics, filter/ottl]
          exporters: [debug]
        logs:
          receivers: [otlp]
          # receivers: [otlp, hostmetrics,  envoyals]
          processors: [resource, resourcedetection/system, k8sattributes, memory_limiter, transform/metrics, filter/ottl]
          exporters: [debug]
        traces:
          receivers: [otlp]
          # receivers: [otlp, hostmetrics,  envoyals]
          processors: [resource, resourcedetection/system, k8sattributes, memory_limiter, transform/metrics, filter/ottl]
          exporters: [debug]