apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: webhook-issuer-tls
  namespace: linkerd
  labels:
    {{- include "linkerd-mesh.labels" . | nindent 4 }}
spec:
  itemPath: {{ .Values.webhookTrustAnchorItemPath }}