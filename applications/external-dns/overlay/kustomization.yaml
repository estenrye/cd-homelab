apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-dns

configMapGenerator:
  - name: env-properties
    env: env.properties

generatorOptions:
  disableNameSuffixHash: true

resources:
  - ../base

replacements:
  - path: replacements/cloudflare-api-token.onepassworditem.yaml
