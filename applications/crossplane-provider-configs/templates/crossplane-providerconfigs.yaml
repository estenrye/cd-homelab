{{- range .Values.crossplaneProviderConfigs }}
---
apiVersion: {{ .apiVersion }}
kind: ProviderConfig
metadata:
  name: {{ .name}}
  labels:
    {{- include "crossplane-provider-configs.labels" . | nindent 4 }}
spec:
  {{ .spec | toYaml | nindent 2 }}
{{- end }}