apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cert-manager-cluster-issuer
  namespace: argocd
spec:
  generators:
  - clusters:
      values:
        acme_email: esten.rye@ryezone.com
        dns01_onepassword_item_path: vaults/Home_Lab/items/letsencrypt-dns01-credentials_cert-manager.rye.ninja
        dns_zones: '["rye.ninja"]'
  template:
    metadata:
      name: '{{name}}-cert-manager-cluster-issuer' # 'name' field of the Secret
    spec:
      project: "platform"
      source:
        repoURL: git@github.com:estenrye/cd-homelab.git
        targetRevision: HEAD
        path: applications/clusterissuer-dns01
        helm:
          releaseName: letsencrypt-prod
          valuesObject:
            acme:
              email: "{{acme_email}}"
            dns_zones: "{{dns_zones}}"
            credentials:
              itemPath: "{{dns01_onepassword_item_path}}"
              cloudflare:
                enabled: true
      destination:
        server: '{{server}}' # 'server' field of the secret
        namespace: cert-manager
      syncPolicy:
        automated: # automated sync by default retries failed attempts 5 times with following delays between attempts ( 5s, 10s, 20s, 40s, 80s ); retry controlled using `retry` field.
          prune: true # Specifies if resources should be pruned during auto-syncing ( false by default ).
          selfHeal: true # Specifies if partial app sync should be executed when resources are changed only in target Kubernetes cluster and no git change detected ( false by default ).
          allowEmpty: false # Allows deleting all application resources during automatic syncing ( false by default ).
        syncOptions:     # Sync options which modifies sync behavior
        - Validate=false # disables resource validation (equivalent to 'kubectl apply --validate=false') ( true by default ).
        - CreateNamespace=true # Namespace Auto-Creation ensures that namespace specified as the application destination exists in the destination cluster.
        - PrunePropagationPolicy=foreground # Supported policies are background, foreground and orphan.
        - PruneLast=true # Allow the ability for resource pruning to happen as a final, implicit wave of a sync operation
        - RespectIgnoreDifferences=true # When syncing changes, respect fields ignored by the ignoreDifferences configuration
        - ServerSideApply=true

        # The retry feature is available since v1.7
        retry:
          limit: 5 # number of failed sync attempt retries; unlimited number of attempts if less than 0
          backoff:
            duration: 5s # the amount to back off. Default unit is seconds, but could also be a duration (e.g. "2m", "1h")
            factor: 2 # a factor to multiply the base duration after each failed retry
            maxDuration: 3m # the maximum amount of time allowed for the backoff strategy

      # RevisionHistoryLimit limits the number of items kept in the application's revision history, which is used for
      # informational purposes as well as for rollbacks to previous versions. This should only be changed in exceptional
      # circumstances. Setting to zero will store no history. This will reduce storage used. Increasing will increase the
      # space used to store the history, so we do not recommend increasing it.
      revisionHistoryLimit: 10