apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minecraft
spec:
  endpoints:
  - interval: 15s
    port: prometheus
  selector:
    matchLabels:
      app: minecraft-minecraft-prometheus