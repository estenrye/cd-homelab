apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager-config
  namespace: argocd
spec:
  project: service
  source:
    chart: cert-manager-install
    repoURL: https://estenrye.github.io/helm-charts
    targetRevision: 0.1.0
    helm:
      values:
        cert-manager.enabled: false
        acme_email: esten.rye+stream@example.com
        cloudflare_api_token: fooo-baaar
  destination:
    name: Stable Cluster-276834
    namespace: cert-manager
